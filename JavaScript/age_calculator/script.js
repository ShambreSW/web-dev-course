var ageInYears = prompt("How old are you?");
var ageInDays = ageInYears * 365.25;

// Adding the .25 factors in a leap year for every four years of our calculation.

alert(ageInYears + " years is roughly " + ageInDays + " days.");
